# Changelog

## v1.0.8 (2025-06-14)

### IV级 / I~III级 分区独立控制
将混合模式拆分为两个独立分区，各自控制不同等级的武装购买策略。

- **IV级武装**：3 种模式（全买 / 按勾选 / 不买），仅控制 4 级紫色武装
  - 「按勾选」时展示独立 21 武装复选框，旁带折叠按钮
  - 「全买」「不买」时复选框自动隐藏
- **I~III级武装**：2 种模式（购买勾选 / 不购买），仅控制 1~3 级武装
  - 「购买勾选」时展示独立 21 武装复选框，旁带折叠按钮
  - 「不购买」时复选框自动隐藏
- 使用 Notebook 标签页切换两个分区，窗口高度不变
- 日志输出区分 `IV:` 和 `I~III:` 标签

### 默认配置更新
- 默认勾选从 11 个武装缩减为 3 个：蓄能爆破炮、分束机炮、六联导弹发射器
- IV 默认模式：按勾选（原「购买所有」）
- 两个分区默认勾选一致

### 内部清理
- `local/` 开发文件整理至 `dev/`
- 清理根目录临时文件（build/dist/debug_screenshots/tesseract_portable）
- 完善 dev_notes.md 开发文档

### 热修复
- 锁定 `rapidocr==3.7.0`，修复 PyInstaller 打包后 ONNX 模型文件缺失（Actions 安装 3.8.3 导致 `--collect-all` 漏打包 `.onnx`）

## v1.0.7 ⚠ 重大更新

### 检测方式变更
- ❌ 移除：模板匹配（cv2.matchTemplate）
- ✅ 新增：投影分行 + ONNX 文字识别（RapidOCR PP-OCRv4）
- 准确率：216 张实测截图，名字全部正确，价格识别基本可用
- 速度：41ms/卡，6 卡约 260ms

### GUI 新增
- 配置区域：参考图 → 预览确认 → 3×2 网格
- 使用指南弹窗（GIF 动画）
- 窗口居中
- 启动自动管理员提权
- 紧凑日志：每卡一行 + 阶段耗时

> 💡 建议保留上一版本，如遇识别问题可回退

## v1.0.6 (2025-06-02)

- `arm_thresholds` 配置项：可为任意武装单独设置匹配阈值
- 蓄能爆破炮默认阈值 0.92，其他武装 0.90
- GUI 控件变化实时保存到 config.json（500ms 防抖）

## v1.0.5 (2025-06-02)

- IV 级选择改为 3 个互斥模式：购买所有 / 仅购买勾选武装 / 不购买
- 武装选择 (I/II/III 级) 与 IV 级选择分开为两个区域
- 修复 IV 级"仅购买勾选武装"模式下 1/2/3 级卡片被错误跳过
- IV 级判断优先于 1/2/3 级

## v1.0.4 (2025-06-02)

- ARM_THRESHOLD 提高到 0.90，修复蓄能射束炮误识别为蓄能爆破炮
- 卡片检测仅限框选区域内 3x2 等分
- 未配卡片/刷新区域时禁用开始，GUI 红字提示
- 刷新按钮仅用框选坐标，删除模板匹配

## v1.0.3 (2025-06-02)

- EXE 文件名带版本号
- 打包加入 `--uac-admin` 自动管理员提权

## v1.0.2 (2025-06-02)

- 刷新按钮优先使用框选坐标
- 更新默认配置为 v1.0.1

## v1.0.1 (2025-06-02)

- F8 全局热键停止
- `run.bat` 本地一键运行
- 默认配置使用用户实际设置

## v1.0.0 (2025-05-27)

- 模板匹配识别武装和已售状态
- 紫色检测 IV 级武装
- Win32 API 模拟键鼠（SendInput + keybd_event）
- tkinter GUI 置顶窗口
- 全选 / IV级 / 按名称三种筛选模式
- 设置自动保存，下次启动恢复
- GitHub Actions 自动打包 Release
